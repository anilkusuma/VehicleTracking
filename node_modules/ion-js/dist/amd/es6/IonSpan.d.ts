export declare class StringSpan {
    private _src;
    private _pos;
    private _start;
    private _limit;
    private _line;
    private _old_line_start;
    private _line_start;
    constructor(src: string, start?: number, len?: number);
    position(): number;
    getRemaining(): number;
    setRemaining(r: number): void;
    is_empty(): boolean;
    next(): number;
    _inc_line(): void;
    unread(ch: number): void;
    peek(): number;
    skip(dist: number): void;
    valueAt(ii: number): number;
    charAt(ii: number): string;
    offset(): number;
    locator(pos: number): number[];
    error_line(start: number, end: number): any[];
    clone(start: number, len: number): StringSpan;
}
export declare class BinarySpan {
    private _src;
    private _pos;
    private _start;
    private _limit;
    constructor(src: number[], start?: number, len?: number);
    error_line(start: number, end: number): any[];
    locator(pos: number): number[];
    position(): number;
    getRemaining(): number;
    setRemaining(r: number): void;
    is_empty(): boolean;
    next(): number;
    unread(b: number): void;
    peek(): number;
    skip(dist: number): void;
    valueAt(ii: number): number;
    clone(start: number, len: number): BinarySpan;
    write(b: number): never;
}
