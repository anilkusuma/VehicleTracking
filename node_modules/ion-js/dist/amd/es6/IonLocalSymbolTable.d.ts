import { Import } from "./IonImport";
export declare class LocalSymbolTable {
    private _import;
    private offset;
    private _symbols;
    private index;
    constructor(_import?: Import, symbols?: string[]);
    getSymbolId(symbol_: string): number;
    addSymbol(symbol_: string): number;
    getSymbol(symbolId: number): string;
    readonly symbols: string[];
    readonly import: Import;
}
export declare function defaultLocalSymbolTable(): LocalSymbolTable;
