import { SharedSymbolTable } from "./IonSharedSymbolTable";
export declare class Import {
    private readonly _parent;
    private readonly _symbolTable;
    private readonly _offset;
    private readonly _length;
    private readonly index;
    constructor(_parent: Import, _symbolTable: SharedSymbolTable, length?: number);
    getSymbol(symbolId: number): string;
    getSymbolId(symbol_: string): number;
    readonly parent: Import;
    readonly offset: number;
    readonly length: number;
    readonly symbolTable: SharedSymbolTable;
}
