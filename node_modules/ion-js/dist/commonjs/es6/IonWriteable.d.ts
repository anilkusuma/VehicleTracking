export declare class Writeable {
    private bufferGrowthSize;
    private buffers;
    private index;
    private written;
    constructor(bufferInitialSize?: number, bufferGrowthSize?: number);
    writeByte(b: number): void;
    writeBytes(b: number[] | Uint8Array, offset?: number, length?: number): void;
    writeStream(it: IterableIterator<number>): void;
    private capacity();
    private allocate();
    getBytes(): number[];
}
